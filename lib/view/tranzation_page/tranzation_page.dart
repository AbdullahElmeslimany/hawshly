import 'package:flutter/material.dart';

class TranzationPage extends StatelessWidget {
  const TranzationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
